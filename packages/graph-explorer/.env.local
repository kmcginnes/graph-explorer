# Local sandbox overrides. Pins the dev ports so the Vite dev server and the
# proxy server line up with the pre-seeded default connection, and keeps the
# proxy off the privileged port 80 (no sudo needed on macOS/Linux).
GRAPH_EXP_DEV_PORT=5173
PROXY_SERVER_HTTP_PORT=8181
