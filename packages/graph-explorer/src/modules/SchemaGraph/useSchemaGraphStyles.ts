import useGraphStyles from "@/modules/GraphViewer/useGraphStyles";

/** Returns styles for the schema graph, reusing the existing graph styles. */
export function useSchemaGraphStyles() {
  return useGraphStyles();
}
